# Contributing

1. Fork the repository
2. Create a feature branch
3. Make changes following Biome formatting
4. Run `bun run lint` and `bun run build`
5. Submit a pull request

## Commit Convention

Use conventional commits: `feat:`, `fix:`, `chore:`, `docs:`, `refactor:`
